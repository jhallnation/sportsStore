<div class='col-md-4'>
  <div class="card">
    <%= image_tag item_img(item.thumb_image, 'thumb'), :class => 'home-img-size' %>
    <div class='item-card'><%= link_to item.title, item %> / $<%= item.price %> / <%= link_to"Add to shopping cart!" %></div>
    <br>
  </div>
</div>